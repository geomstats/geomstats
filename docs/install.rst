Install Geomstats
=================


You can install geomstats with ``pip3`` as follows::

    pip3 install geomstats

You should choose your backend by setting the environment variable GEOMSTATS_BACKEND to numpy, tensorflow or pytorch::

    export GEOMSTATS_BACKEND=numpy
